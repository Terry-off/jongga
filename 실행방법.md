# 실행 방법 (윈도우)

가장 쉬운 길은 **더블클릭 두 번**이에요. 차근차근 따라오세요.

---

## 0단계. 파이썬 설치 (최초 1회, 5분)

1. https://www.python.org/downloads/ 접속 → 노란 **Download Python** 버튼 클릭
2. 받은 파일 실행 → **맨 아래 "Add python.exe to PATH" 체크** ✅ (이거 안 하면 안 돼요)
3. **Install Now** 클릭 → 끝나면 닫기

이미 깔려 있으면 건너뛰세요.

---

## 1단계. 프로그램 내려받기 (최초 1회)

### 방법 A — ZIP 다운로드 (깃 몰라도 됨, 추천)
1. 브라우저에서 저장소 페이지 열기
2. 초록색 **Code** 버튼 → **Download ZIP**
3. 받은 ZIP을 **압축 풀기** → 예: `바탕화면\jongga`

### 방법 B — 깃이 있으면
```
git clone -b claude/wizardly-cori-nxdrbb <저장소 주소>
```

---

## 2단계. 실행 (더블클릭)

압축 푼 폴더 안 `scripts` 폴더를 열면 파일 두 개가 있어요:

| 파일 | 언제 |
|---|---|
| **데모보기.bat** | 그냥 화면이 어떻게 생겼는지 구경 (키·인터넷 불필요) |
| **실행하기.bat** | 진짜 데이터로 사용 (한국투자증권 키 필요) |

원하는 걸 **더블클릭**하세요.

- 처음 한 번은 "환경 준비" 때문에 1~2분 걸려요 (검은 창에 글자가 주르륵 — 정상이에요).
- **실행하기.bat**을 처음 누르면 메모장이 열려요 → 아래 키를 붙여넣고 **저장(Ctrl+S) 후 닫기**:
  ```
  KIS_APP_KEY=발급받은 앱키
  KIS_APP_SECRET=발급받은 시크릿
  ```
  (DART·네이버 키도 있으면 같이 넣으면 뉴스·테마 점수까지 나와요. 없어도 작동해요.)
- 잠시 뒤 브라우저가 열려요. 안 열리면 주소창에 **http://127.0.0.1:8765** 직접 입력.
- **끄려면** 검은 창에서 `Ctrl+C` 또는 창 닫기.

> ⚠️ 검은 창은 켜 둔 동안만 작동해요. 창을 닫으면 프로그램도 꺼져요.

---

## 3단계. 화면 둘러보기

- **추천 화면**: 오늘(또는 과거 날짜)의 추천 종목·점수·이유·비중
- **📒 일지**: 매매 기록 → 킬스위치·검증 리포트가 자동 계산
- **⚙️ 설정**: 모든 필터 기준을 바꾸고, 한 번에 기본값으로 복원

> 실전 추천은 **장 마감 무렵(오후 2시 30분 ~ 3시 30분)**에 봐야 의미가 있어요.
> 그 외 시간엔 데이터가 완성되지 않아 비어 보일 수 있어요.

---

## 매일 자동으로 데이터 모으기 (선택, 권장)

분봉·시간외 같은 데이터는 그날 지나가면 다시 못 받아요. 매일 18:10 자동 수집을 걸어두면
나중에 어떤 과거 날짜든 그대로 다시 볼 수 있어요. → [scripts/windows_scheduler.md](scripts/windows_scheduler.md)

---

## 안 될 때

| 증상 | 해결 |
|---|---|
| "파이썬이 없어요" | 0단계 다시 — 설치 때 PATH 체크 빠뜨렸으면 재설치 |
| 검은 창이 깜빡하고 사라짐 | `scripts` 폴더에서 빈 곳 Shift+우클릭 → "여기에 PowerShell 열기" → `.\실행하기.bat` 입력 후 Enter (오류 메시지 확인) |
| 브라우저에 "연결할 수 없음" | 검은 창에 글자가 다 올라올 때까지 1~2초 기다린 뒤 새로고침(F5) |
| 한국투자증권 연결 실패 | 실전투자용 키가 맞는지 확인 (모의투자 키는 거래대금 순위 API를 막아요) |
