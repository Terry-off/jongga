# 종가매매 종목 추천 프로그램 — 설계서 v1.0

> **이 프로그램의 한 문장 정의**
> 매 거래일, 시장이 돈(거래대금)·명분(재료)·구조(섹터·대장주·캔들)로 인정한 종목만 걸러서,
> "왜 추천하는지/왜 탈락했는지"를 쉬운 말로 설명하고, 틀려도 계좌가 망가지지 않을 비중까지 계산해 주는 도구.
>
> **면책**: 이 프로그램은 추천(스크리닝) 도구이며 매매를 실행하지 않는다. 모든 투자 판단과 책임은 사용자 본인에게 있다.
> 기반 문서: 『실패하지 않는 종가매매 시스템 설계서 v2』(이하 "설계서 v2")

---

## 1. 확정된 설계 결정 (사용자 합의 완료)

| # | 결정 사항 | 선택 |
|---|---|---|
| 1 | 화면 형태 | **웹 대시보드** (브라우저, 토스 스타일 카드 UI) |
| 2 | 재료(뉴스·공시) 분석 | **전자동** — DART 공시 + 네이버 뉴스 + 키워드 규칙으로 A/B/C 자동 등급, 근거 표시 + 수동 교정 가능 |
| 3 | 한국투자증권 API | **실전투자 키 사용** (시세 조회 전용, 매매 권한 불필요) |
| 4 | 과거 날짜 조회 | **당일 중심 + 가능한 과거** — 수집기 가동 이후는 완전 재현, 이전 과거는 가능한 항목만 채점하고 나머지는 "확인 불가" 표시 |
| 5 | 실행 환경 | **윈도우 PC** (작업 스케줄러로 일일 수집, 꺼진 날은 다음 실행 때 보충 수집) |
| 6 | 장중 알림 | **없음** — 사용자가 직접 화면에서 "지금 스캔" 조회 (알림은 추후 추가 가능 구조로 설계) |

---

## 2. 전체 구조

```
┌─ 데이터 수집 계층 ─────────────────────────────────┐
│ ① 한국투자증권 API  시세·일봉·분봉·거래대금 순위·수급·     │
│                    경보 플래그·시간외 체결                │
│ ② DART 공시 API    수주·실적·증자·CB (무료 공식)          │
│ ③ 네이버 뉴스 API   종목 뉴스 제목·시각 (무료 공식)        │
│ ④ 테마 매핑        네이버 금융 테마 분류, 일 1회 수집      │
│ ⑤ 이벤트 캘린더    FOMC·CPI·고용지표·국내 휴장일 내장      │
│                  + 화면에서 직접 추가/수정               │
└──────────────────┬───────────────────────────────┘
                   ▼
┌─ 저장 계층 ──────────────────────────────────────┐
│ SQLite 파일 1개 (data/jongga.sqlite3)               │
│ 매 거래일 18:10 자동 스냅샷 — 시간외 체결·경보 지정처럼   │
│ 지나가면 다시 못 보는 데이터를 보존 → 과거 조회의 원천    │
└──────────────────┬───────────────────────────────┘
                   ▼
┌─ 판단 엔진 ──────────────────────────────────────┐
│ 시장 신호등 → 유니버스 압축 → 베토 9개 → 100점 채점       │
│ → 판정(85/75/65) → 비중 계산 → 쉬운 말 설명 생성          │
│ 모든 기준값은 설정에서 읽음 (기본값 = 설계서 v2의 수치)    │
└──────────────────┬───────────────────────────────┘
                   ▼
┌─ 웹 대시보드 (FastAPI + 서버 템플릿) ────────────────┐
│ 추천 화면 / 설정 화면 / (2단계) 매매일지·킬스위치          │
└──────────────────────────────────────────────────┘
```

**기술 스택**: Python 3.11+ / FastAPI + Jinja 템플릿(빌드 도구 없음) / SQLite / 캔들 차트는 TradingView lightweight-charts(CDN). 전부 무료.

---

## 3. 데이터 소스와 한계 (정직하게)

| 설계서 v2의 기준 | 데이터 출처 | 한계와 대응 |
|---|---|---|
| 거래대금, 일봉/캔들 | KIS 일봉·순위 API | 문제없음 |
| 오후 분봉 흐름 | KIS 분봉 API | 당일은 완전. 과거 분봉은 조회 범위 제한 → **일일 스냅샷으로 보존** |
| 수급(외국인·기관) | KIS 투자자 동향 | 장중은 잠정치, 확정치는 다음 날 보정 수집 |
| 위험종목(투자경고 등) | KIS 종목 상태 플래그 | **현재 시점만 조회 가능** → 매일 스냅샷으로 이력화 |
| 시간외 단일가 체결 | KIS 시간외 API | **당일만 조회 가능** → 18:10 스냅샷 필수 |
| 재료/뉴스 등급 | DART 공시 + 네이버 뉴스 | KIS에는 뉴스 없음. 자동 등급의 오판 가능성 → 근거 표시 + 수동 교정 |
| 테마/섹터 동조 | 네이버 금융 테마 수집 | 수집 실패 시 전일 매핑 + KRX 업종으로 대체, 화면에 기준일 표시 |
| 매크로 이벤트 | 내장 연간 캘린더 | FOMC·CPI·고용지표는 연초 일정 공개됨. 사용자 추가/수정 가능 |

**보수 원칙**: 확인할 수 없는 데이터는 추측해서 채우지 않는다. "확인 불가" 뱃지를 달고, 해당 배점을 제외한 환산 점수를 함께 표시한다 (예: "82점 / 90점 만점 환산").

---

## 4. 판단 파이프라인

### 4.1 시장 신호등 (설계서 v2 제2부 — 매매 금지일)

코스피·코스닥 지수와 캘린더로 그날의 매매 환경을 🟢가능 / 🟡주의 / 🔴쉬는 날로 판정하고 이유를 문장으로 표시한다.

| 점검 항목 | 계산 방법 |
|---|---|
| 오후에도 저점을 낮추는가 | 지수 분봉: 14시 이후 당일 최저가 갱신 + 마감가가 당일 가격대 하위 25% |
| 주도 섹터가 있는가 | 테마 동조 판정(4.6)에서 동반 3종목 이상 그룹 존재 여부 |
| 막판 수직 급등만 보이는가 | 등락률 상위 종목 중 "14시 이후 급등" 베토 해당 비율 |
| 익일 대형 이벤트 | 이벤트 캘린더 조회 (FOMC·CPI·고용지표 등) |
| 금요일·연휴 전일 | 달력 + KRX 휴장일 |

🔴여도 종목 분석은 보여주되, 화면 전체에 "오늘은 쉬는 날" 경고를 크게 표시한다. (쉬는 것도 포지션)

### 4.2 유니버스 압축

전 종목(~2,700개)을 전수 조사하지 않는다. **거래대금 순위 + 등락률 상위** API로 "오늘 돈이 몰린 종목" 100~200개만 추출한다. 내일 갭을 띄우는 것은 신규 매수자이고, 신규 매수자는 거래대금에 먼저 나타난다 — 이 압축 자체가 1차 필터다.

### 4.3 베토 9개 — 하나라도 걸리면 점수 계산 없이 탈락 (설계서 v2 제4부 STEP A)

| # | 조건 | 계산식 | 기본값(설정 키) |
|---|---|---|---|
| 1 | 재료 불명확 | 재료 엔진이 A/B/C 어느 등급도 못 찾음 | 수동 등급 입력으로 구제 가능 |
| 2 | 거래대금 미달 | 당일 거래대금 < 유형별 기준 | 중소형 300억 / 테마 500억 / 대형 순위 50위 (`trading_value.*`) |
| 3 | 장대 윗꼬리 | (고가−종가) ÷ (고가−저가) > 기준 | 0.40 (`candle.max_upper_shadow_ratio`) |
| 3′ | 종가 캔들 중간 이하 | (종가−저가) ÷ (고가−저가) < 기준 | 0.50 (`candle.min_close_position`) |
| 4 | 막판 수직 급등 | 14시 이후 상승분 ÷ 당일 상승분 > 기준 (당일 +5%↑일 때만 점검) | 0.60 (`late_surge.*`) |
| 5 | 과열 테마 후발주 | 테마 주도주 3일 누적 > 기준 이고 본인이 대장주 아님 | +20% (`sector.overheat_3day_gain`) |
| 6 | 위험종목 | KIS 경보코드(주의·경고·위험·단기과열·관리·거래정지) 또는 최근 N일 내 유증·CB 공시 | 30일 (`risk_stock.cb_lookback_days`) |
| 7 | 익일 대형 이벤트 | 캘린더에서 다음 거래일 조회 | 내장 + 사용자 추가 |
| 8 | 금요일·연휴 전일 | B급 이하 재료면 탈락, A급도 85점↑만 절반 비중 | ON (`calendar.friday_requires_a_grade`) |
| 9 | 매매 금지일 | 시장 신호등 🔴 | — |

탈락 종목도 버리지 않고 "아깝게 탈락한 종목" 섹션에 탈락 사유 1줄과 함께 기록한다 (학습 데이터).

### 4.4 100점 채점 (설계서 v2 제4부 STEP B)

| 항목 | 배점 | 세부 계산 |
|---|---|---|
| 시장 분위기 | 15 | 지수 양봉/오후 회복 7 + 오후 저점 높임 4 + 위험 이벤트 없음 4 |
| 재료 강도 | 20 | A급 20 / B급+섹터동조 12 / C급+동반조건 6 |
| 섹터 동조 | 15 | 같은 테마 +3%↑ 동반 3종목 8 + 본인이 대장주 7 |
| 거래대금 | 15 | 유형별 기준 충족 10 + 전일 대비 3배 이상 5 |
| 일봉 위치 | 15 | 60일/52주 고가 −5% 이내 6 + 종가위치 0.7↑ 5 + 5·10·20일선 위 2 + 검증 패턴(20일선 지지 후 거래량 재반등 등) 2 |
| 오후 분봉 | 10 | 13~15시 저점 우상향 5 + 15:10 이후 거래량 재유입 3 + VWAP 위 2 |
| 수급 | 10 | 외인+기관 양매수 10 / 한쪽만 6 / 개인 단독 매수+외기 매도 0~2 |

- **소형주 보정**(설계서 v2 조건 6): 시총 기준(기본 3,000억) 미만이면 수급 10점을 거래대금·분봉에 자동 재배분 (기본 ON, `sector.smallcap_supply_realloc`)
- **판정**: 85↑ 정상 비중 / 75~84 절반 비중 / 65~74 관찰만 / 미만 제외 (`score.*`)

### 4.5 재료 등급 엔진 (전자동 + 수동 교정)

```
공시(DART)                            뉴스(네이버 검색 API)
 단일판매·공급계약(수주)      → A       정부·정책·장관·승인·FDA·수주·
 실적 잠정공시(서프라이즈 방향) → A       흑자전환·사상최대        → A 후보
 신규시설투자·대규모 투자      → A~B     증권사·목표가 상향·리포트·
 무상증자                  → B        산업 성장               → B 후보
 유상증자·CB 발행           → 베토6     [단독]·추진·검토·~설      → C 후보
                                      매칭 없음               → 재료 미확인
            └──────────────┬──────────────┘
                           ▼
        최종 등급 = 공시·뉴스 중 높은 쪽
        C급은 "거래대금 + 섹터동조 + 종가 고가권" 3개 동시 충족 때만 인정
```

- 카드에 판정 근거(공시명·기사 제목·링크·보도 시각)를 그대로 표시
- 사용자가 등급을 수동 변경하면 즉시 재채점, 교정 이력은 저장 → 키워드 규칙 개선에 사용 (M4 화면에서)
- 키워드 사전은 설정 파일(config/material_keywords.yaml)로 분리해 사용자가 직접 추가/삭제 가능
- **테마 데이터 합류 전 보수 규칙**: B급은 섹터 동조 확인이 필요하므로 테마 수집(M3.5) 전까지 12점이 아닌 6점만 반영.
  C급의 동반 조건 중 확인 불가능한 항목(테마 동조)은 '제한 인정' 메모와 함께 통과시키되 확인 가능한 조건(거래대금·종가 고가권)은 엄격 적용.
- 조회 실패(네트워크 등)는 '재료 없음'(베토)이 아니라 '확인 불가'(만점 제외)로 구분 처리

### 4.6 테마 동조·대장주 판별 (설계서 v2 조건 2)

- **테마 분류**: 네이버 금융 테마(약 250개)를 하루 1회 수집해 종목↔테마 매핑 저장. 실패 시 전일 매핑 + KRX 업종 대체, 화면에 "테마 데이터: ○월 ○일 기준" 표시
- **동조**: 같은 테마에서 당일 등락률 ≥ +3% 이고 거래대금 ≥ 50억인 종목 3개 이상
- **대장주**: 테마 내 거래대금 1위 **그리고** 상승률 상위 3위 이내 (신고가 선도는 일봉 점수에 반영되므로 중복 계산 안 함)
- 대장주가 아니면 "후발주" 경고, 과열 테마(베토 5)면 탈락

### 4.7 비중 계산기 (설계서 v2 제1부)

```
1R = 계좌 자산 × risk_per_trade_pct (기본 0.4%)
최대 매수금액 = 1R ÷ 최악 갭 (대형·실적주 5% / 테마·중소형주 10%)
점수 연동: 85점↑ → 100% / 75~84 → 50%
```
계좌 자산은 설정 화면에서 직접 입력(API 잔고 연동 안 함 — 시세 전용 키 사용 원칙). 결과는 카드에 "이 종목은 최대 ○○만 원"으로 표시.

### 4.8 과거 날짜 조회 원칙

- 수집기 가동 이후 날짜: 모든 항목 완전 재현
- 가동 이전 과거: 일봉·수급·캔들·거래대금은 채점, 분봉·시간외·당시 경보는 "확인 불가" 뱃지 + 환산 점수 병기
- 없는 데이터를 추측으로 채우지 않는다

---

## 5. 필터 설정 카탈로그

모든 기준값은 `config/defaults.yaml`에 있고, 설정 화면에서 변경 → DB에 저장(원본 yaml은 불변) → "기본값 복원" 버튼으로 되돌리기. 항목마다 설계서 v2의 근거 문장을 함께 표시한다.

| 그룹 | 키 | 기본값 | 근거 (설계서 v2) |
|---|---|---|---|
| 계좌 | `account.total_asset` | 1,000만 원 | 비중 계산용, 사용자 입력 |
| 계좌 | `account.risk_per_trade_pct` | 0.4% | 1R = 0.3~0.5% |
| 계좌 | `account.worst_gap_large_pct / worst_gap_theme_pct` | 5% / 10% | 예상 최악 갭하락 |
| 계좌 | `account.max_positions_per_day` | 2 | 하루 1~2종목 |
| 계좌 | `account.max_total_exposure_pct` | 30% | 하루 총 노출 20~30% |
| 계좌 | `account.max_sector_exposure_pct` | 20% | 동일 섹터 15~20% |
| 거래대금 | `trading_value.midsmall_min_eok` | 300억 | 중소형주 최소 |
| 거래대금 | `trading_value.theme_min_eok` | 500억 | 강한 테마주 최소 |
| 거래대금 | `trading_value.large_rank_max` | 50위 | 대형주 거래대금 순위 |
| 거래대금 | `trading_value.surge_vs_prev_ratio` | 3배 | 전일 대비 급증 가점 |
| 거래대금 | `trading_value.large_cap_threshold_eok` | 2조 | 대형주 구분 시총 |
| 캔들 | `candle.max_upper_shadow_ratio` | 0.40 | 장대 윗꼬리 베토 |
| 캔들 | `candle.min_close_position` | 0.50 | 종가 캔들 중간 이하 베토 |
| 캔들 | `candle.good_close_position` | 0.70 | 종가 고가권 가점 |
| 막판급등 | `late_surge.max_late_gain_share` | 0.60 | 14시 이후 상승 비중 |
| 막판급등 | `late_surge.min_day_gain_for_check` | +5% | 점검 대상 최소 등락률 |
| 섹터 | `sector.sync_min_count` | 3종목 | 동반 상승 최소 |
| 섹터 | `sector.sync_min_change` | +3% | 동조 인정 등락률 |
| 섹터 | `sector.sync_min_value_eok` | 50억 | 동조 인정 최소 거래대금 |
| 섹터 | `sector.overheat_3day_gain` | +20% | 과열 테마 기준 |
| 섹터 | `sector.smallcap_threshold_eok` | 3,000억 | 수급 재배분 시총 기준 |
| 점수 | `score.full_weight_cut / half_weight_cut / watch_cut` | 85 / 75 / 65 | 판정 컷 |
| 위험 | `risk_stock.cb_lookback_days` | 30일 | 유증·CB 공시 소급 |
| 유니버스 | `universe.min_change_rate` | +3% | 상승률 상위 편입 컷 |
| API | `kis.rate_limit_per_sec` | 10 | 실전 한도 20건/초의 절반(안전) |

---

## 6. 화면 설계 (토스 스타일 — 쉬운 말 원칙)

### 6.1 추천 화면 (메인)
- 날짜 선택 (기본 오늘) + 장중이면 "지금 스캔" 버튼 (14:30~15:15 사용 권장 안내)
- **시장 신호등**: 🟢🟡🔴 + 이유 칩 ("내일 새벽 미국 금리 발표가 있어요 — 쉬어가는 날이에요")
- **추천 카드**: 종목명 / 점수(크게) / 뱃지(정상 비중·절반 비중·관찰만) / 쉬운 말 핵심 이유 3줄 / "최대 ○○만 원"
- 카드 펼치면: 점수 7항목 막대그래프(항목별 설명), 베토 체크리스트(✓ 표시), 재료 근거(공시·기사 링크), 일봉 차트, 오후 분봉 차트, 재료 등급 수동 교정 버튼
- **아깝게 탈락한 종목** (접힘): 탈락 사유 1줄씩

### 6.2 설정 화면
- 5번 카탈로그를 그룹별 슬라이더/입력으로, 항목마다 "왜 이 기준인가" 한 줄
- 기본값 복원 버튼 / 이벤트 캘린더 관리(내장 일정 확인 + 직접 추가)
- 재료 키워드 사전 편집

### 6.3 매매일지 (2단계, M6)
- 설계서 v2 제9부 양식 그대로 기록 → R 손익·규칙 준수율 자동 집계
- 킬스위치 상태 표시: 발동 시 추천 화면 전체에 빨간 배너 ("오늘은 시스템이 매매를 막았어요")

### 6.4 쉬운 말 용어 사전 (코드에 내장, 화면 어디서든 ⓘ로 표시)
| 용어 | 쉬운 말 |
|---|---|
| 거래대금 | 오늘 이 주식이 사고팔린 돈의 총액. 사람이 얼마나 몰렸는지 보여줘요 |
| 윗꼬리 | 장중엔 더 올라갔다가 밀려 내려온 흔적. 위에서 판 사람이 많다는 뜻이에요 |
| 갭 | 어제 마감 가격과 오늘 시작 가격의 차이 |
| 수급 | 외국인·기관·개인 중 누가 사고 팔았는지 |
| 동시호가 | 마감 직전 10분간 주문만 모아서 한 번에 체결하는 시간 |
| 대장주 | 같은 테마에서 돈이 가장 많이 몰린 1등 종목 |
| 1R | 한 번 틀렸을 때 잃어도 되는 최대 금액 |

---

## 7. DB 스키마 개요 (SQLite)

| 테이블 | 내용 | 채워지는 시점 |
|---|---|---|
| `universe_snapshot` | 일자별 후보 종목(가격·등락률·거래대금·출처) | M1 |
| `daily_price` | 종목 일봉 (수정주가) | M2 |
| `minute_price` | 종목 분봉 (당일 수집분 보존) | M2/M5 |
| `investor_flow` | 일별 수급 (잠정→확정 보정) | M2/M5 |
| `stock_flags` | 일자별 경보·과열·관리 플래그 스냅샷 | M5 |
| `after_hours` | 시간외 단일가 체결 스냅샷 | M5 |
| `theme_map` | 종목↔테마 매핑 (수집일 기준) | M2 |
| `materials` | 재료 판정 결과(등급·근거·수동 교정 이력) | M3 |
| `screening_result` | 일자별 채점 결과(점수·베토·설명) — 복기용 | M2 |
| `settings_override` | 사용자 변경 설정값 | M4 |
| `events_calendar` | 매크로 이벤트·휴장일 | M2 |
| `trade_journal` | 매매일지·킬스위치 상태 | M6 |
| `collect_log` | 수집 이력 (보충 수집 판단용) | M1 |

## 8. 일일 운영 스케줄 (윈도우)

| 시각 | 작업 | 방식 |
|---|---|---|
| 18:10 (평일) | 일일 스냅샷 수집 (`python -m jongga collect`) | 윈도우 작업 스케줄러 등록 (설치 안내 제공) |
| 다음 날 08:30 | 수급 확정치 보정 수집 | 동일 스케줄러 |
| 수동 | 장중 "지금 스캔" (14:30~15:15 권장) | 웹 화면 버튼 |
| 자동 | PC가 꺼져 있던 날 → 다음 실행 때 `collect_log` 대조 후 보충 수집 (보충 불가 항목은 "확인 불가" 처리) | 수집기 내장 |

## 9. 마일스톤

| 단계 | 내용 | 완료 확인 | 상태 |
|---|---|---|---|
| M1 | KIS 연결(토큰·호출 제한)·DB·설정 시스템·유니버스 수집 CLI | `python -m jongga smoke` / `universe` 출력 | 완료 |
| M2 | 베토+채점 엔진 (재료·테마 제외 65점 체계 우선 가동) + 시장 신호등 + 추천 CLI | `recommend --demo` 시연 + 테스트 15건 | 완료 |
| M3 | 재료 엔진(DART+네이버) 합류 — 키워드 등급·C급 동반조건 검증·유증/CB 위험 연동 | `material` 명령 + 테스트 20건 | 완료 (테마 동조는 M3.5) |
| M4 | 웹 대시보드 + 쉬운 말 설명 + 설정 화면 | 브라우저 확인 | |
| M5 | 일일 스냅샷 수집기·과거 조회·보충 수집 | 스케줄러 등록 후 익일 확인 | |
| M6 | 매매일지·킬스위치·검증 리포트 (설계서 v2 제9부) | | |

**필요한 키** (전부 무료): ① KIS 실전투자 앱키·시크릿 (보유 ✓), ② DART 오픈API 키 (M3 전까지), ③ 네이버 개발자센터 키 (M3 전까지)
